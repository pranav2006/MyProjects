{
    "builds": [
      {
        "src": "main.py",
        "use": "@vercel/python"
      }
    ],
    "buildCommand": "python3 -m ensurepip && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "main.py"
      }
    ]
  }